name: e8
processed_dir: data/processed/kuairand_sessions
sessions_filename: sessions.parquet
output_dir: artifacts/e8
survival_horizons: [1, 3, 5, 10]
primary_horizon: 5
base_rate_low: 0.2
base_rate_high: 0.8
duration_quantile_bins: 5
bootstrap:
  resamples: 2000
  confidence: 0.95
  seed: 0
decision:
  min_abs_slope: 0.005
  mde_z: 2.8
metrics_filename: metrics.json
predictions_filename: predictions.csv
manifest_filename: run_manifest.json
